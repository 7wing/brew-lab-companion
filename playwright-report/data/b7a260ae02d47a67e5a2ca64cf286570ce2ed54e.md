# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: onboarding.spec.ts >> Onboarding >> Complete Onboarding
- Location: e2e/onboarding.spec.ts:11:3

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected: "http://localhost:8080/new-brew"
Received: "http://localhost:8080/onboarding"
Timeout:  5000ms

Call log:
  - Expect "toHaveURL" with timeout 5000ms
    14 × unexpected value "http://localhost:8080/onboarding"

```

```yaml
- region "Notifications (F8)":
  - list
- region "Notifications alt+T"
- text: Step 1 of 4 What you brew
- heading "Welcome to Homebrew Haven 🍺" [level=1]
- paragraph: What do you like to brew? (select all that apply)
- button "Beer"
- button "Cider"
- button "Mead"
- button "Kombucha"
- button "Wine"
- button "Sourdough"
- button "Continue"
- button "Skip for now"
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test';
  2   | import {
  3   |   getProfileByUserId,
  4   |   deleteUserById,
  5   |   createTestUser,
  6   | } from './helpers/supabase';
  7   | 
  8   | test.describe('Onboarding', () => {
  9   |   test.setTimeout(60000);
  10  | 
  11  |   test('Complete Onboarding', async ({ page }) => {
  12  |     const email = `test-complete-${Date.now()}@example.com`;
  13  |     const password = 'TestPass123!';
  14  | 
  15  |     // 1. Create user via admin API with confirmed email
  16  |     const user = await createTestUser(email, password);
  17  | 
  18  |     // 2. Go to auth page and sign in
  19  |     await page.goto('/auth');
  20  | 
  21  |     const signInTab = page.getByRole('tab', { name: /sign in/i });
  22  |     if (await signInTab.isVisible().catch(() => false)) {
  23  |       await signInTab.click();
  24  |     }
  25  | 
  26  |     await page.locator('[name="email"]').fill(email);
  27  |     await page.locator('[name="password"]').fill(password);
  28  | 
  29  |     const submitButton = page.locator('form button[type="submit"]');
  30  |     await submitButton.click();
  31  | 
  32  |     // 3. Wait for redirect to onboarding
  33  |     await page.waitForURL(/\/onboarding/, { timeout: 15000 });
  34  |     await expect(page).toHaveURL(/\/onboarding/);
  35  | 
  36  |     // 4. Step 1: select brew types
  37  |     await page.getByRole('button', { name: 'Beer' }).click();
  38  |     await page.getByRole('button', { name: 'Mead' }).click();
  39  |     await page.getByRole('button', { name: 'Continue' }).click();
  40  | 
  41  |     // 5. Step 2: select experience
  42  |     await page.getByRole('button', { name: '3–5 years' }).click();
  43  |     await page.getByRole('button', { name: 'Continue' }).click();
  44  | 
  45  |     // 6. Step 3: log first batch
  46  |     await page.getByRole('button', { name: /yes, let's log it/i }).click();
  47  | 
  48  |     // 7. Step 4: follow seed brewers
  49  |     const hopsHarmonyCard = page.locator('div.glass-panel').filter({ hasText: 'Hops Harmony' });
  50  |     await hopsHarmonyCard.getByRole('button', { name: 'Follow' }).click();
  51  |     await page.getByRole('button', { name: 'Go to Brew Bench' }).click();
  52  | 
  53  |     // 8. Wait for navigation (redirects to /new-brew when logFirstBatch is true)
  54  |     await page.waitForURL('http://localhost:8080/new-brew', { timeout: 15000 });
> 55  |     await expect(page).toHaveURL('http://localhost:8080/new-brew');
      |                        ^ Error: expect(page).toHaveURL(expected) failed
  56  | 
  57  |     // 9. Verify profile in DB
  58  |     const profile = await getProfileByUserId(user.id);
  59  |     expect(profile.brew_types).toContain('beer');
  60  |     expect(profile.brew_types).toContain('mead');
  61  |     expect(profile.experience_level).toBe('3-5_years');
  62  |     expect(profile.onboarding_completed).toBe(true);
  63  | 
  64  |     // 10. Navigate to /profile and verify it loads
  65  |     await page.goto('/profile');
  66  |     await expect(page).toHaveURL(/\/profile/);
  67  | 
  68  |     // 11. Cleanup
  69  |     await deleteUserById(user.id);
  70  |   });
  71  | 
  72  |   test('Skip Onboarding', async ({ page }) => {
  73  |     const email = `test-skip-${Date.now()}@example.com`;
  74  |     const password = 'TestPass123!';
  75  | 
  76  |     // 1. Create user via admin API with confirmed email
  77  |     const user = await createTestUser(email, password);
  78  | 
  79  |     // 2. Go to auth page and sign in
  80  |     await page.goto('/auth');
  81  | 
  82  |     const signInTab = page.getByRole('tab', { name: /sign in/i });
  83  |     if (await signInTab.isVisible().catch(() => false)) {
  84  |       await signInTab.click();
  85  |     }
  86  | 
  87  |     await page.locator('[name="email"]').fill(email);
  88  |     await page.locator('[name="password"]').fill(password);
  89  | 
  90  |     const submitButton = page.locator('form button[type="submit"]');
  91  |     await submitButton.click();
  92  | 
  93  |     // 3. Wait for onboarding
  94  |     await page.waitForURL(/\/onboarding/, { timeout: 15000 });
  95  |     await expect(page).toHaveURL(/\/onboarding/);
  96  | 
  97  |     // 4. Skip all steps
  98  |     await page.getByRole('button', { name: 'Skip for now' }).click();
  99  |     await page.getByRole('button', { name: 'Skip for now' }).click();
  100 |     await page.getByRole('button', { name: 'Skip for now' }).click();
  101 |     await page.getByRole('button', { name: 'Skip for now' }).click();
  102 | 
  103 |     // 5. Wait for redirect to /
  104 |     await page.waitForURL('http://localhost:8080/', { timeout: 15000 });
  105 |     await expect(page).toHaveURL('http://localhost:8080/');
  106 | 
  107 |     // 6. Verify profile in DB
  108 |     const profile = await getProfileByUserId(user.id);
  109 |     expect(profile.onboarding_completed).toBe(true);
  110 |     const brewTypes = profile.brew_types;
  111 |     expect(brewTypes === null || (Array.isArray(brewTypes) && brewTypes.length === 0)).toBe(true);
  112 |     expect(profile.experience_level === null || profile.experience_level === undefined).toBe(true);
  113 | 
  114 |     // 7. Navigate to /profile and verify it loads
  115 |     await page.goto('/profile');
  116 |     await expect(page).toHaveURL(/\/profile/);
  117 | 
  118 |     // 8. Cleanup
  119 |     await deleteUserById(user.id);
  120 |   });
  121 | 
  122 |   const seedAccounts = [
  123 |     { email: 'hoppy.brewer@example.com', password: 'TestPass123!' },
  124 |     { email: 'kombucha.queen@example.com', password: 'TestPass123!' },
  125 |     { email: 'mead.master@example.com', password: 'TestPass123!' },
  126 |     { email: 'sourdough.sam@example.com', password: 'TestPass123!' },
  127 |     { email: 'cider.sid@example.com', password: 'TestPass123!' },
  128 |   ];
  129 | 
  130 |   for (const account of seedAccounts) {
  131 |     test(`Existing seed account login: ${account.email}`, async ({ page }) => {
  132 |       // 1. Go to auth page
  133 |       await page.goto('/auth');
  134 | 
  135 |       // 2. Make sure on Sign in tab
  136 |       const signInTab = page.getByRole('tab', { name: /sign in/i });
  137 |       if (await signInTab.isVisible().catch(() => false)) {
  138 |         await signInTab.click();
  139 |       }
  140 | 
  141 |       // 3. Fill credentials
  142 |       await page.locator('[name="email"]').fill(account.email);
  143 |       await page.locator('[name="password"]').fill(account.password);
  144 | 
  145 |       // 4. Click sign-in submit
  146 |       const submitButton = page.locator('form button[type="submit"]');
  147 |       await submitButton.click();
  148 | 
  149 |       // 5. Wait for redirect — should be /, not /onboarding
  150 |       await page.waitForURL('http://localhost:8080/', { timeout: 15000 });
  151 |       await expect(page).toHaveURL('http://localhost:8080/');
  152 |     });
  153 |   }
  154 | });
  155 | 
```